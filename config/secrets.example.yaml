# NÃO preencher senha aqui e NÃO versionar secrets reais.
# Use Windows Credential Manager/keyring no ambiente controlado.
credentials:
  vrops:
    username: "SEU_USUARIO"
    password_source: "keyring"
  vcenter:
    username: "SEU_USUARIO"
    password_source: "keyring"
